# Core dependencies
python-dotenv==1.0.0
requests==2.31.0

# Database
pymysql==1.1.1
sqlalchemy==2.0.32
aiomysql==0.2.0

# HTTP client with async support
httpx==0.27.0

# Retry and resilience
tenacity==8.2.3

# Async processing (asyncio는 표준 라이브러리이므로 제거)
nest_asyncio==1.6.0

# Data processing (버전 범위 최적화)
numpy==1.26.4
pandas==2.2.2

# Excel handling
openpyxl==3.1.2

# Email handling (더 안정적인 패키지로 교체)
smtplib2==0.2.1

# Parallel processing enhancements (concurrent-futures는 Python 3.2+에서 표준 라이브러리)
# concurrent-futures 제거 (표준 라이브러리)
# multiprocessing-logging 제거 (호환성 이슈)

# Performance monitoring
psutil==5.9.8
memory-profiler==0.61.0

# Configuration validation
pydantic==2.7.4
PyYAML==6.0.1

# CLI enhancements
click==8.1.7
rich==13.7.1
tqdm==4.66.4

# Logging enhancements
colorlog==6.8.2
structlog==24.2.0

# Testing and development (optional - 개발용이므로 주석 처리)
# pytest==8.2.2
# pytest-asyncio==0.23.7
# pytest-cov==5.0.0

# Type checking (development - 개발용이므로 주석 처리)
# mypy==1.10.1
# types-requests==2.32.0.20240622 